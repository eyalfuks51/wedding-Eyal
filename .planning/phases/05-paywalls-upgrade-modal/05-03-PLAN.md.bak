---
phase: 05-paywalls-upgrade-modal
plan: "03"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/pages/AutomationTimeline.tsx
autonomous: true
requirements:
  - PAY-01

must_haves:
  truths:
    - "Draft event user navigating to /dashboard/timeline sees a premium placeholder card instead of the pipeline"
    - "The placeholder includes DashboardNav so users retain navigation context"
    - "The placeholder explains Timeline is a premium feature with an upgrade CTA"
    - "Active event users see the normal Timeline pipeline"
  artifacts:
    - path: "src/pages/AutomationTimeline.tsx"
      provides: "Premium placeholder for draft users replacing Navigate redirect"
      contains: "TimelinePremiumPlaceholder"
  key_links:
    - from: "src/pages/AutomationTimeline.tsx"
      to: "src/components/ui/glass-card.tsx"
      via: "GlassCard import for placeholder card"
      pattern: "import.*GlassCard.*glass-card"
    - from: "src/pages/AutomationTimeline.tsx"
      to: "src/hooks/useFeatureAccess.ts"
      via: "canAccessTimeline check"
      pattern: "canAccessTimeline"
---

<objective>
Replace the AutomationTimeline redirect for draft users with a full-page premium placeholder that communicates the value of Timeline and prompts upgrade.

Purpose: Instead of silently redirecting draft users away from Timeline (confusing), show them what they're missing with a clear upgrade path. This is the only route-level paywall (PAY-01).
Output: AutomationTimeline.tsx renders a premium placeholder when canAccessTimeline is false.
</objective>

<execution_context>
@C:/Users/Eyal/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Eyal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-paywalls-upgrade-modal/05-RESEARCH.md
@.planning/phases/05-paywalls-upgrade-modal/05-01-SUMMARY.md

<interfaces>
<!-- GlassCard from src/components/ui/glass-card.tsx -->
```typescript
export function GlassCard({ className, style, ...props }: React.ComponentProps<'div'>): JSX.Element;
export function GlassCardContent({ className, style, ...props }: React.ComponentProps<'div'>): JSX.Element;
```

<!-- UpgradeModal from Plan 01 -->
```typescript
interface UpgradeModalProps {
  isOpen: boolean;
  onClose: () => void;
  onUpgradeClick: () => void;
}
export default function UpgradeModal(props: UpgradeModalProps): JSX.Element | null;
```

<!-- useFeatureAccess -->
```typescript
export function useFeatureAccess(): {
  canAccessTimeline: boolean;
  // ...other flags
};
```

<!-- DashboardNav -->
```typescript
export default function DashboardNav(): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Timeline redirect with premium placeholder</name>
  <files>src/pages/AutomationTimeline.tsx</files>
  <action>
In `src/pages/AutomationTimeline.tsx`, replace the existing `<Navigate>` redirect for draft users with an inline premium placeholder.

**1. Find the existing gate check.** It currently looks like:
```typescript
if (!canAccessTimeline) return <Navigate to="/dashboard/settings" replace />;
```
Remove the `Navigate` import if it becomes unused after this change.

**2. Replace with a TimelinePremiumPlaceholder block.** Define this as either a local component or inline JSX. Since it's only used here, inline JSX is fine:

```typescript
if (!canAccessTimeline) {
  return (
    <div className="min-h-screen bg-slate-50 text-slate-900 font-brand" dir="rtl">
      {/* Reuse the SAME page header/layout wrapper that the real timeline uses */}
      <header className="...">  {/* Copy the existing header structure */}
        <DashboardNav />
      </header>
      <main className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex items-center justify-center py-20">
          <GlassCard className="max-w-md w-full rounded-3xl text-center p-8">
            {/* Icon — use Calendar or Clock from lucide-react */}
            <div className="mx-auto w-16 h-16 bg-violet-100 rounded-full flex items-center justify-center mb-4">
              <CalendarIcon className="w-8 h-8 text-violet-600" />
            </div>
            {/* Title */}
            <h2 className="font-danidin text-2xl text-slate-900 mb-2">ציר הזמן</h2>
            {/* Description */}
            <p className="text-slate-600 mb-6 leading-relaxed">
              נהלו את כל תהליך האוטומציה של ההודעות — תזכורות, ניגנובים, והודעות לוגיסטיקה — הכל ממקום אחד.
            </p>
            {/* Upgrade CTA button */}
            <button
              onClick={() => setUpgradeOpen(true)}
              className="bg-violet-600 hover:bg-violet-700 text-white rounded-xl px-6 py-3 w-full font-medium transition-colors"
            >
              שדרגו לגרסה המלאה
            </button>
          </GlassCard>
        </div>
      </main>
      {/* UpgradeModal */}
      <UpgradeModal
        isOpen={upgradeOpen}
        onClose={() => setUpgradeOpen(false)}
        onUpgradeClick={() => {
          setUpgradeOpen(false);
          // Use whatever toast mechanism exists in this file.
          // If no toast exists, add a simple one: state + setTimeout pattern from Dashboard.
        }}
      />
    </div>
  );
}
```

**3. Add required state and imports:**
- `import UpgradeModal from '@/components/ui/UpgradeModal';`
- `import { GlassCard } from '@/components/ui/glass-card';`
- `const [upgradeOpen, setUpgradeOpen] = useState(false);`
- A `Calendar` or `Clock` icon import from lucide-react

**4. Match the existing page wrapper.** Read the AutomationTimeline component to understand its current page shell (header, background, container classes). The placeholder must use the SAME outer shell so it looks like the same page, just with placeholder content instead of the pipeline. DashboardNav must be in the same position.

**5. Toast for Coming Soon.** AutomationTimeline.tsx already has a toast mechanism (check for existing toast state). If it exists, reuse it. If not, add the simple pattern: `const [toast, setToast] = useState<string | null>(null)` + `setTimeout(() => setToast(null), 3000)` + render a fixed toast div at z-60.

**IMPORTANT:** The `upgradeOpen` state and the early return must be structured so React hooks are not called conditionally. Declare all state hooks BEFORE the `if (!canAccessTimeline)` check, or extract the placeholder into a separate component that receives `setUpgradeOpen` as a prop.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/pages/AutomationTimeline.tsx 2>&1 | head -30</automated>
  </verify>
  <done>Draft event users navigating to /dashboard/timeline see a centered GlassCard placeholder explaining Timeline is a premium feature, with DashboardNav visible for navigation. The old Navigate redirect is removed. Active users see the normal pipeline. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for AutomationTimeline.tsx
- No `Navigate` import/usage for the draft user case remains (unless Navigate is used elsewhere in the file)
- The placeholder includes `DashboardNav` component
- The placeholder has a "שדרגו" (upgrade) CTA that opens UpgradeModal
</verification>

<success_criteria>
- Draft event user at /dashboard/timeline sees premium placeholder with GlassCard, not a redirect
- Placeholder has DashboardNav, descriptive text in Hebrew, and an upgrade CTA button
- Clicking the CTA opens UpgradeModal, which shows Coming Soon toast on upgrade click
- Active event users see the normal Timeline pipeline unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/05-paywalls-upgrade-modal/05-03-SUMMARY.md`
</output>
