---
phase: 09-phase6-documentation-retrofix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/06-rsvp-architecture-refactor-tech-debt-cleanup/06-VERIFICATION.md
autonomous: true
requirements: [RSVP-01, RSVP-02, RSVP-03, RSVP-04, RSVP-05]
gap_closure: true

must_haves:
  truths:
    - "RSVP-01 through RSVP-05 are checked as complete in REQUIREMENTS.md"
    - "Traceability table assigns RSVP-01 through RSVP-05 to Phase 6 with status Complete"
    - "Phase 6 planning directory exists with a VERIFICATION.md confirming all 5 requirements are satisfied"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "RSVP requirement checkboxes and traceability"
      contains: "[x] **RSVP-01**"
    - path: ".planning/phases/06-rsvp-architecture-refactor-tech-debt-cleanup/06-VERIFICATION.md"
      provides: "Retroactive verification report for Phase 6"
      contains: "score: 5/5"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "ROADMAP.md Phase 6"
      via: "Traceability table rows for RSVP-01 through RSVP-05 pointing to Phase 6"
      pattern: "RSVP-0[1-5].*Phase 6.*Complete"
---

<objective>
Register the 5 orphaned RSVP requirements as complete and create retroactive Phase 6 verification artifacts.

Purpose: Phase 6 was executed outside the GSD workflow. The v1.0 milestone audit confirmed all 5 RSVP requirements are functionally implemented, but REQUIREMENTS.md checkboxes and traceability point to "Phase 9 / Pending" instead of "Phase 6 / Complete", and Phase 6 has no planning directory or VERIFICATION.md. This plan closes that documentation gap.

Output: Updated REQUIREMENTS.md with checked RSVP requirements, new Phase 6 directory with 06-VERIFICATION.md.
</objective>

<execution_context>
@C:/Users/Eyal/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Eyal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/09-phase6-documentation-retrofix/09-RESEARCH.md
@.planning/phases/07-testing-qa-infrastructure/07-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REQUIREMENTS.md checkboxes and traceability for RSVP requirements</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Edit `.planning/REQUIREMENTS.md` to reflect that RSVP-01 through RSVP-05 are complete and belong to Phase 6:

1. **Checkboxes (lines 80-84):** Change each `- [ ]` to `- [x]` for RSVP-01 through RSVP-05. Do NOT change the descriptions -- they are already correct.

2. **Traceability table (lines 168-172):** Change each row from:
   - `| RSVP-0X | Phase 9 | Pending |` to `| RSVP-0X | Phase 6 | Complete |`

3. **Do NOT change** the coverage count (47 total, 47 mapped) -- it is already correct.
4. **Do NOT change** any other sections or requirements.
  </action>
  <verify>
    <automated>grep -c "\[x\] \*\*RSVP-0" .planning/REQUIREMENTS.md | grep -q "5" && grep -c "RSVP-0.*Phase 6.*Complete" .planning/REQUIREMENTS.md | grep -q "5" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>All 5 RSVP checkboxes are [x], all 5 traceability rows show "Phase 6 | Complete"</done>
</task>

<task type="auto">
  <name>Task 2: Create Phase 6 directory and retroactive VERIFICATION.md</name>
  <files>.planning/phases/06-rsvp-architecture-refactor-tech-debt-cleanup/06-VERIFICATION.md</files>
  <action>
1. Create directory `.planning/phases/06-rsvp-architecture-refactor-tech-debt-cleanup/`

2. Create `06-VERIFICATION.md` following the exact format used by `07-VERIFICATION.md`. This is a RETROACTIVE verification -- Phase 6 was executed outside the GSD workflow and the v1.0 audit confirmed all requirements are satisfied.

**Frontmatter:**
- `phase: 06-rsvp-architecture-refactor-tech-debt-cleanup`
- `verified:` current ISO timestamp
- `status: passed`
- `score: 5/5 must-haves verified`
- `re_verification: false` (first formal verification even though code shipped earlier)
- `gaps: []`
- `human_verification: []`

**Observable Truths (5):**
1. Unmatched guest RSVP is saved with `match_status = 'unmatched'` -- Evidence: `arrival_permits` table has `match_status` column; DB trigger `sync_arrival_to_invitation` sets 'unmatched' when no phone match found in `invitations`
2. Matched guest RSVP is saved with `match_status = 'matched'` and invitation updated -- Evidence: Trigger populates `invitation_id` FK and updates `confirmed_pax` on the matched invitation
3. Legacy trigger `sync_rsvp_to_invitations` is removed -- Evidence: Trigger and function no longer exist in database (ROADMAP task 6.3)
4. Google Sheets webhook trigger `sheets_sync_trigger` is removed -- Evidence: Trigger removed from database (ROADMAP task 6.4)
5. Admin dashboard surfaces unmatched RSVPs for review -- Evidence: Dashboard.tsx contains UnmatchedBanner and resolution UI (ROADMAP task 6.5, audit confirms "UnmatchedBanner + resolution UI wired")

**Required Artifacts:**
- `arrival_permits` table columns: `invitation_id` (uuid FK), `match_status` (text)
- DB trigger `sync_arrival_to_invitation` with match/unmatch logic
- `src/pages/Dashboard.tsx` with unmatched RSVP UI

**Requirements Coverage table:** Use "ROADMAP.md Phase 6 tasks" as Source Plan (no plan files exist for Phase 6). All 5 requirements: SATISFIED.

**Anti-Patterns Found:** None.

**Add a note in the Summary section:** "This is a retroactive verification. Phase 6 was executed outside the GSD workflow as manual database migrations and UI changes. The v1.0 milestone audit (2026-03-18) confirmed all 5 requirements are functionally implemented. This VERIFICATION.md formally records that confirmation."
  </action>
  <verify>
    <automated>test -f ".planning/phases/06-rsvp-architecture-refactor-tech-debt-cleanup/06-VERIFICATION.md" && grep -q "status: passed" ".planning/phases/06-rsvp-architecture-refactor-tech-debt-cleanup/06-VERIFICATION.md" && grep -q "score: 5/5" ".planning/phases/06-rsvp-architecture-refactor-tech-debt-cleanup/06-VERIFICATION.md" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Phase 6 directory exists with 06-VERIFICATION.md showing status: passed, score: 5/5, all 5 RSVP requirements listed as SATISFIED</done>
</task>

</tasks>

<verification>
1. `grep -c "\[x\] \*\*RSVP-0" .planning/REQUIREMENTS.md` returns 5
2. `grep -c "RSVP-0.*Phase 6.*Complete" .planning/REQUIREMENTS.md` returns 5
3. `test -d .planning/phases/06-rsvp-architecture-refactor-tech-debt-cleanup/` succeeds
4. `grep "status: passed" .planning/phases/06-rsvp-architecture-refactor-tech-debt-cleanup/06-VERIFICATION.md` succeeds
5. No code files were modified (documentation-only phase)
</verification>

<success_criteria>
- All 5 RSVP requirements marked [x] in REQUIREMENTS.md
- All 5 traceability rows show Phase 6 / Complete
- Phase 6 directory exists with 06-VERIFICATION.md (status: passed, 5/5 score)
- Zero code files changed
</success_criteria>

<output>
After completion, create `.planning/phases/09-phase6-documentation-retrofix/09-01-SUMMARY.md`
</output>
