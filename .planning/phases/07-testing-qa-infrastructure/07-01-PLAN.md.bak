---
phase: 07-testing-qa-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vite.config.js
  - package.json
  - src/lib/phone.ts
  - src/lib/phone.test.ts
  - src/lib/guest-excel.ts
  - CLAUDE.md
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03

must_haves:
  truths:
    - "npm run test executes Vitest and exits successfully"
    - "Phone normalization edge cases (05x, +972, 972, dashes, spaces, mixed) all pass"
    - "isValidPhone correctly accepts 9-12 digit numbers and rejects others"
    - "CLAUDE.md mandates test verification before any phase completion"
  artifacts:
    - path: "vite.config.js"
      provides: "Vitest inline test configuration"
      contains: "test:"
    - path: "package.json"
      provides: "test and test:watch npm scripts"
      contains: "vitest run"
    - path: "src/lib/phone.ts"
      provides: "Exported normalizePhone and isValidPhone functions"
      exports: ["normalizePhone", "isValidPhone"]
    - path: "src/lib/phone.test.ts"
      provides: "Comprehensive phone normalization unit tests"
      min_lines: 40
    - path: "src/lib/guest-excel.ts"
      provides: "Imports phone functions from phone.ts instead of inline"
      contains: "import.*from.*phone"
    - path: "CLAUDE.md"
      provides: "Mandatory testing standards section"
      contains: "NO PHASE IS COMPLETE WITHOUT TESTS"
  key_links:
    - from: "src/lib/phone.test.ts"
      to: "src/lib/phone.ts"
      via: "import { normalizePhone, isValidPhone }"
      pattern: "import.*normalizePhone.*isValidPhone.*from.*phone"
    - from: "src/lib/guest-excel.ts"
      to: "src/lib/phone.ts"
      via: "import for phone utilities"
      pattern: "import.*from.*phone"
    - from: "package.json"
      to: "vitest"
      via: "test script"
      pattern: "vitest run"
---

<objective>
Set up Vitest unit testing infrastructure and write comprehensive phone normalization tests.

Purpose: Establish a testing foundation so all future development phases have a mandatory quality gate. Phone normalization is the most critical pure-logic path (RSVP matching depends on it).
Output: Working `npm run test` command, phone utility module with full test coverage, updated CLAUDE.md testing mandate.
</objective>

<execution_context>
@C:/Users/Eyal/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Eyal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-testing-qa-infrastructure/07-RESEARCH.md

@vite.config.js
@package.json
@src/lib/guest-excel.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Configure Vitest, extract phone utilities, write tests</name>
  <files>vite.config.js, package.json, src/lib/phone.ts, src/lib/phone.test.ts, src/lib/guest-excel.ts</files>
  <behavior>
    - normalizePhone("0521234567") returns "972521234567"
    - normalizePhone("972521234567") returns "972521234567" (passthrough)
    - normalizePhone("+972521234567") returns "972521234567" (strips +)
    - normalizePhone("052-123-4567") returns "972521234567" (strips dashes)
    - normalizePhone("052 123 4567") returns "972521234567" (strips spaces)
    - normalizePhone("+972-52 123-4567") returns "972521234567" (mixed separators)
    - normalizePhone("521234567") returns "521234567" (no prefix, passthrough)
    - normalizePhone("0501234567") returns "972501234567" (050 prefix)
    - normalizePhone("0541234567") returns "972541234567" (054 prefix)
    - isValidPhone("0521234567") returns true (10 digits)
    - isValidPhone("972521234567") returns true (12 digits)
    - isValidPhone("+972521234567") returns true (12 digits after strip)
    - isValidPhone("12345") returns false (too short)
    - isValidPhone("1234567890123") returns false (too long, 13 digits)
    - isValidPhone("052-123-4567") returns true (10 digits after strip)
  </behavior>
  <action>
    1. Add Vitest inline config to vite.config.js:
       - Add `/// <reference types="vitest/config" />` at top
       - Add `test` block inside defineConfig: `{ globals: true, environment: 'node', include: ['src/**/*.test.{ts,tsx}'], exclude: ['node_modules', 'tests'] }`
       - The `tests/` exclusion prevents Playwright spec files from being picked up by Vitest

    2. Add npm scripts to package.json:
       - `"test": "vitest run"` (one-shot for CI/verification)
       - `"test:watch": "vitest"` (dev watch mode)
       - Keep existing `"test:e2e": "playwright test"`

    3. Create src/lib/phone.ts:
       - Export `normalizePhone(raw: string): string` -- strips non-digits, converts 0-prefix to 972-prefix, passes through 972-prefix, returns digits as-is otherwise
       - Export `isValidPhone(raw: string): boolean` -- strips non-digits, checks length 9-12
       - These are extracted verbatim from src/lib/guest-excel.ts lines 59-69

    4. Update src/lib/guest-excel.ts:
       - Remove the inline `normalizePhone` and `isValidPhone` function definitions (lines 59-69)
       - Add `import { normalizePhone, isValidPhone } from './phone';` at the top with other imports
       - All existing call sites in parseGuestExcel remain unchanged

    5. Create src/lib/phone.test.ts:
       - Write tests for all behaviors listed above using describe/it/expect from vitest
       - Group into `describe('normalizePhone')` and `describe('isValidPhone')` blocks
       - Include a comment block noting the DB `phone_core` function has DIFFERENT output (core digits only, not 972-prefixed) and explaining why both are correct for their respective contexts
  </action>
  <verify>
    <automated>cd C:/dev/github/personal/Wedding-Eyal && npm run test</automated>
  </verify>
  <done>
    - `npm run test` runs Vitest and all phone normalization tests pass
    - src/lib/phone.ts exports normalizePhone and isValidPhone
    - src/lib/guest-excel.ts imports from phone.ts (no inline phone functions)
    - No Playwright test collision (tests/ dir excluded from Vitest)
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify CLAUDE.md testing mandate is complete</name>
  <files>CLAUDE.md</files>
  <action>
    CLAUDE.md already has a "Testing Standards (Mandatory)" section (lines 313-321) that covers:
    - Critical rule banner about no phase being complete without tests
    - Unit test mandate with `npm run test`
    - E2E test mandate with `npm run test:e2e`
    - Verification before completion requirement
    - Test file location conventions

    Verify this section is accurate and complete. If the `npm run test` script name matches what was added in Task 1, no changes needed. If any detail is stale or missing (e.g., script names don't match, test file locations are wrong), update accordingly.

    The section should confirm:
    - `npm run test` for Vitest unit tests
    - `npm run test:e2e` for Playwright E2E tests
    - Unit tests co-located as `*.test.ts` in `src/`
    - E2E tests in `tests/*.spec.ts`
    - Both must pass before any phase is marked complete
  </action>
  <verify>
    <automated>cd C:/dev/github/personal/Wedding-Eyal && grep -c "NO PHASE IS COMPLETE WITHOUT TESTS" CLAUDE.md</automated>
  </verify>
  <done>
    - CLAUDE.md contains a mandatory testing standards section
    - Section references correct script names (npm run test, npm run test:e2e)
    - Section specifies test file locations matching project conventions
  </done>
</task>

</tasks>

<verification>
1. `npm run test` executes and all tests pass (no failures, no Playwright collision)
2. `npm run test:watch` starts Vitest in watch mode (manual spot-check)
3. `src/lib/guest-excel.ts` still functions correctly (imports phone.ts, no broken references)
4. CLAUDE.md has the testing mandate section
</verification>

<success_criteria>
- Vitest configured inline in vite.config.js with node environment
- `npm run test` and `npm run test:watch` scripts exist and work
- Phone normalization logic extracted to src/lib/phone.ts with named exports
- src/lib/guest-excel.ts imports from phone.ts (no duplication)
- Comprehensive tests in src/lib/phone.test.ts covering all Israeli phone formats
- CLAUDE.md mandates testing verification for all future phases
</success_criteria>

<output>
After completion, create `.planning/phases/07-testing-qa-infrastructure/07-01-SUMMARY.md`
</output>
